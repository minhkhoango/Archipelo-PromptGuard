{
  "name": "sentinel-extension",
  "publisher": "your-publisher-name",
  "displayName": "Sentinel Chat Participant",
  "description": "A security-focused chat participant that scans prompts for secrets",
  "version": "0.1.0",
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "AI",
    "Chat"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "chatParticipants": [
      {
        "id": "sentinel.security",
        "fullName": "Sentinel",
        "name": "sentinel",
        "description": "🛡️ Security-focused assistant that scans for secrets and provides secure coding guidance",
        "isSticky": true,
        "commands": [
          {
            "name": "scan",
            "description": "Scan the provided text for potential secrets and security issues"
          },
          {
            "name": "help",
            "description": "Show available security scanning features"
          }
        ]
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.95.0",
    "@types/node": "^20",
    "typescript": "^5.8.2"
  }
}
